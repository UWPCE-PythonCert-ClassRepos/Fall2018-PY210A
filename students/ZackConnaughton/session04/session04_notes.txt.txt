return an empty list as a default argument

def test(arg=None):
	if arg == None:
		arg = []
	return arg
	
Don't remove items from a list as you loop through it
Use a copy of the list - list[:]

loop through a list and remove based on criteria
ind = 0
for something in list[:]:
	if (criteria for delete)
		del list[ind]
	else:
		ind += 1

list.sort - changes list
sorted(list) - sorted list output, does not change list

tim sort - look it up

Jae Kim - arg parser dash arguments in the command line code